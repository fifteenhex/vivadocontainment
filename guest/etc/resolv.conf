# No DNS on purpose: the guest reaches one address, the broker, and it is
# numeric. With no nameserver a lookup fails at once rather than waiting on a
# blocked query -- which is what Vivado's update and WebTalk checks would do
# on every single run otherwise.
options timeout:1 attempts:1
